import { ToEntriesPipe } from './to-entries-pipe';

describe('ToEntriesPipe', () => {
  it('create an instance', () => {
    const pipe = new ToEntriesPipe();
    expect(pipe).toBeTruthy();
  });
});
